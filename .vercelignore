# Owned OSS source we ported from — not needed by the Next.js build (excluded from tsc too)
backend/
python/
reference/
.claude/
*.log
# MCP connector package + Claude Science skills — shipped to GitHub, not to the web app.
# Anchored to root (leading slash) so they do NOT also match app/api/mcp or lib/mcp.
/mcp/
/skills/
