export function NarrowScopeSection() {
  return (
    <section className="mb-10">
      <h2 className="text-lg font-semibold">Deliberately narrow</h2>
      <p className="mt-3 text-sm leading-relaxed text-ink/80">
        PaperTrail checks clinical-trial efficacy claims against PubMed and ClinicalTrials.gov —
        not preprints, press releases, or arbitrary statements. A narrow tool that is fully
        correct is more useful to a researcher than a broad one that is only approximately right.
      </p>
    </section>
  );
}
